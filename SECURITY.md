# 安全策略

## 范围

本仓库是 **RFC / 方案文档**，不含可执行代码。因此没有传统意义上的
"代码漏洞"，安全相关的是**方案本身的安全属性与法律边界**。

## 方案的安全设计（见 `SOLUTION.md`）

- **仅回环入站**：sing-box 的 mixed 入站绑定 `127.0.0.1`，不对外网暴露
  代理端口。
- **子进程强绑定**：主程序用 Windows **Job Object** 绑定 sing-box 子进程，
  主程序退出即回收，**零残留**（不用会漏进程的裸 `start`）。
- **无私有加密**：不使用、也不逆向私有加密驱动；协议即公开的标准代理协议。
- **单一出口**：不做多跳、不做流量中转，节点由用户自己的账号获取。

## 法律与合规

> **使用者自行负责合规。**

- 本方案**不提供节点、不提供订阅、不绕过任何付费机制**。
- 请在**你所在地区法律允许的范围内**使用；使用他人服务时遵守其服务条款。
- 仓库维护者不对使用者的行为承担责任（见 [LICENSE](LICENSE)）。

## 报告问题

安全 / 合规相关问题请**不要**开公开 issue，用私密通道：

<https://github.com/X1F2Y3/devpn-link/security/advisories/new>

## 不在范围内

- 上游 sing-box 自身的漏洞（请报给上游）。
- "在某地能不能用" 这类法律咨询 —— 请咨询专业人士，本仓库不提供。
